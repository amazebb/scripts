#!/usr/bin/env zsh
# Helper function used by fzf-man

{ (($#)) && apropos -s "$1" . || man -k .; } |
    awk -F' - ' '!seen[$1]++' |
    sort -r |
    # \033[36;1m = cyan + bold, \033[0m = reset, \033[34m = blue
    awk '{ $1="\033[36;1m" $1; $2="\033[0m\033[34m" $2; } 1'
